<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.CS422PanicPlanner" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Main background of the windows -->
        <item name="android:windowBackground">#121212</item>
        <!-- Background for containers/cards (used in bg_container.xml) -->
        <item name="android:colorBackground">#1E1E1E</item>

        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/purple_200</item>

        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">#BDBDBD</item>

        <!-- Ensure status bar is also dark -->
        <item name="android:statusBarColor">#121212</item>

        <item name="alertDialogTheme">@style/Theme.App.AlertDialog</item>
        <item name="android:alertDialogTheme">@style/Theme.App.AlertDialog</item>

        <!-- Button Styles -->
        <item name="buttonStyle">@style/Widget.App.Button</item>
        <item name="android:buttonStyleSmall">@style/Widget.App.Button.Small</item>
    </style>

    <style name="Widget.App.Button" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/bg_button_rounded</item>
        <item name="android:backgroundTint">@color/purple_200</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Widget.App.Button.Small" parent="Widget.AppCompat.Button.Small">
        <item name="android:background">@drawable/bg_button_rounded</item>
        <item name="android:backgroundTint">@color/purple_200</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.App.AlertDialog" parent="Theme.AppCompat.Dialog.Alert">
        <item name="colorAccent">@color/purple_200</item>
    </style>
</resources>